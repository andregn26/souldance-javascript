import { Box } from "@mui/material"
import { styled } from "@mui/system"

const MainSectionContainerNoNav = styled(Box)({
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  minHeight: "100vh",
})

export default MainSectionContainerNoNav
